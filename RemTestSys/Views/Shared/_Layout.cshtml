<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="author" content="Maksim Kasyanenko" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/reset.css" />
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    <header class="site-header">
        <div class="layout between-panel">
            <img src="~/images/site-logo.svg">
            @if (ViewData["Page"].ToString() == "Registration")
            {
                <p>beta</p>
            }
            else
            {
                <nav>
                    <ul class="right-panel">
                        <li class="@(ViewData["Page"].ToString() == "AvailableTests" ? "current-page" : "")"><a asp-action="AvailableTests" asp-controller="Student">Тести</a></li>
                        <li class="@(ViewData["Page"].ToString() == "Results" ? "current-page" : "")"><a asp-action="Results" asp-controller="Student">Результати</a></li>
                        @if (ViewData["Page"].ToString() != "AccountInfo")
                        {
                            <li><a asp-action="AccountInfo" asp-controller="Account">@ViewBag.StudentFullName</a></li>
                        }
                    </ul>
                </nav>
            }
        </div>
    </header>
    <div class="layout content">
        @RenderBody()
    </div>
    <footer class="site-footer">
        <div class="layout between-panel">
            <a asp-action="Home" asp-controller="Editor">Edit</a>
            <span>&copy;2021</span>
            <a href="https://github.com/MaksimKasyanenko/RemTestSys" class="github-logo"><img src="~/images/github-logo.png" /></a>
        </div>
    </footer>
</body>
</html>
